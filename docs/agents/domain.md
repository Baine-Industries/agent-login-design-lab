# Domain docs

This repository uses a single domain context at `CONTEXT.md` and local Wayfinder artifacts under `.scratch/`.

Before changing product language, read `CONTEXT.md`, the engine/UI boundary contract, and the relevant Wayfinder map. Keep `CONTEXT.md` limited to stable domain vocabulary and boundaries; put unresolved questions and implementation detail in the map or child tickets.
